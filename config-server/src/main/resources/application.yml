server:
  port: 8889
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/jimband/configdemo.git
          search-paths: respo
          username:
          password:
      label: master

