server:
  port: 8888
eureka:
  instance:
    hostname: localhost
  client:
#自己不用注册
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
